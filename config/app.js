/**
 * Configurable values for apps
 *
 */

const listQuestionTypes = ["checkbox", "dropdown", "radio"];

const textQuestionTypes = ["short", "long"];

export default { listQuestionTypes, textQuestionTypes };
